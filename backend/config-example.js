// Configuração de exemplo para o sistema
// Copie este arquivo para config.js e configure suas chaves

module.exports = {
  // Google Gemini API Key
  // Para obter uma chave gratuita, acesse: https://makersuite.google.com/app/apikey
  GEMINI_API_KEY: process.env.GEMINI_API_KEY || 'your_gemini_api_key_here',
  
  // Para testar sem IA, use uma chave vazia:
  // GEMINI_API_KEY: '',
};
